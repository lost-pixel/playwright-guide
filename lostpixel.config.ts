export const config = {
  customShots: {
    // 📒 currentShotsPath should be matching the one above from playwright test
    currentShotsPath: "./lost-pixel",
  },
  lostPixelProjectId: "clhn3rcr209btod0e553qbwa8",
  apiKey: process.env.LOST_PIXEL_API_KEY,
};
